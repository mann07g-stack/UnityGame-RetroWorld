using UnityEngine;

public static class PlayerSpawnData
{
    // Static variables to hold spawn info across scenes
    public static Vector3 spawnPosition = Vector3.zero;
    public static Vector3 spawnRotation = Vector3.zero;
}